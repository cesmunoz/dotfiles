#!/bin/bash
# Usage: cm-install
# Install & updates the configuration

set -e

gum style \
    --foreground 45 --border-foreground 51 --border double \
    --align center --width 50 --margin "0 0" --padding "1 4" \
    'CM Dotfiles - Install & Update'

cd
source ~/dev/cm/dotfiles/install.sh